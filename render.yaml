services:
  - type: web
    name: genmitra-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn src.dashboard.app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
